{% extends 'base.html.twig' %}

{% block title %}Title{% endblock %}

{% block body %}

    {% for user in users %}
        {{ user.name }}
        {{ user.gender }}
        {{ user.eyesColor }}
        <br/>
    {% endfor %}
{% endblock %}
